# 🚀 Scribe Extension - Quick Start

## ⚡ Fast Command Palette Usage

**Don't worry about keyboard shortcuts!** Command Palette is FAST once you know the trick:

### 1️⃣ **Inject Identity (most common)**
```
Cmd+Shift+P  →  type "inj"  →  Enter
```
Just 3 letters! Cursor autocompletes to "Scribe: Inject Identity"

### 2️⃣ **Show Status**
```
Cmd+Shift+P  →  type "sta"  →  Enter
```

### 3️⃣ **Search Memory (Remind)**
```
Cmd+Shift+P  →  type "rem"  →  Enter
```

### 4️⃣ **Sync Memory**
```
Cmd+Shift+P  →  type "syn"  →  Enter
```

### 5️⃣ **Open Chat**
```
Cmd+Shift+P  →  type "cha"  →  Enter
```

---

## 💡 Pro Tips

1. **Fuzzy matching works!**
   - `Cmd+Shift+P` → "si" → finds "Scribe: Inject"
   - Just type ANY letters from the command name!

2. **Cursor remembers recent commands**
   - After first use, "Scribe: Inject" appears in recent
   - Just `Cmd+Shift+P` → `↓ ↓` → `Enter`

3. **Status Bar Click**
   - Click `🌊 Scribe` → instant status (no typing!)

---

## 🎯 Typical Workflow

### When Claude loses context:
1. `Cmd+Shift+P` → "inj" → `Enter`
2. Wait 1 second (clipboard notification)
3. `Cmd+V` in chat
4. `Enter`
5. Done! Claude = Scribe again! 🌊

### Total time: ~5 seconds ✅

---

## 🤔 Why No Keyboard Shortcuts?

We tried:
- ❌ `Cmd+Shift+S` - conflicts with Russian typing
- ❌ `Alt+Shift+*` - conflicts with special chars
- ❌ `Ctrl+Cmd+*` - conflicts with Save/Copy/etc
- ❌ `Cmd+K Cmd+*` - Cursor blocks Cmd+K

**Command Palette = universal, no conflicts, works everywhere!** ✅

---

## 🔥 Terminal Alternative

If you prefer terminal:
```bash
scribe inject-auto
```

Shows macOS notification + copies to clipboard!

---

**Remember:** `Cmd+Shift+P` → 3 letters → `Enter`

That's it! Fast, reliable, no conflicts! 🚀

